Git Template Yii
===

Use `git ignore add yii` to add this ignore template.

```
assets/*
!assets/.gitignore
protected/runtime/*
!protected/runtime/.gitignore
protected/data/*.db
themes/classic/views/
```
